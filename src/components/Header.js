import React, { useState } from "react";
import "../Header.css";

const Header = () => {
  return (
    <div id="header">
      <nav>
        <a href="#">Temp Header Text</a>
      </nav>
    </div>
  );
};

export default Header;
